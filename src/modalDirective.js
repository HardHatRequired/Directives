angular.module('DM').directive('modalDirective', function() {
  return {
    restrict: 'E',
    templateUrl: '/src/views/modaldirective.html'

    link: function(scope, element, attrs) {
      

      // var theModal = document.querySelector('.modal').style;
      // console.log();
    }
  }
})
